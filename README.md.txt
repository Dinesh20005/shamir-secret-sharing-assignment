# Shamir Secret Solver (JavaScript)

## Overview
This project finds the constant `c` of a polynomial from encoded (x, y) points using Lagrange Interpolation.

## How to Run
1. Make sure Node.js is installed.
2. Run:
```bash
node index.js
